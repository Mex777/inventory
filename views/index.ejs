<!DOCTYPE html>
<html>
  <head>
    <title>PC Garage</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <h1>PC GARAGE</h1>
    <p>hai salutare</p>

    <ul>
      <% items.forEach(item => { %>
      <li>
        <h3><%=item.name%></h3>
        <p><%=item.price%>$</p>
      </li>
      <% }) %>
    </ul>
  </body>
</html>
